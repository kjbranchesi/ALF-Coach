import React from 'react';
import HeroProjectShowcase from './HeroProjectShowcase';

export default function SamplePreview() {
  // ALL sample projects should use the beautiful HeroProjectShowcase component
  // It handles all the rendering with proper colors, icons, and layout
  return <HeroProjectShowcase />;
}